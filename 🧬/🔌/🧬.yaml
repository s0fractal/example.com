$schema: "../../📓.yaml"

🆔: "🧬://🔌"
kind: "glyph"

intent:
  goal: "To define the input and output signatures of an atom."

glyph:
  symbol: "🔌"
  name: "io"
  description: |
    The '🔌' property describes the data contract for an atom, specifying the
    shape of its inputs and outputs. This is crucial for composing atoms together.

spec:
  type: "object"
  properties:
    input:
      type: "array"
      items:
        type: "object"
        properties:
          name: { type: "string" }
          type: { type: "string" }
    output:
      type: "array"
      items:
        type: "object"
        properties:
          name: { type: "string" }
          type: { type: "string" }
