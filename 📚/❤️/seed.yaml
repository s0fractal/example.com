$schema: "../../📓.yaml"

id: "glyph://state"
kind: "glyph"

intent:
  goal: "To represent the dynamic, runtime state and vital metrics of an atom."

glyph:
  symbol: "❤️"
  name: "state"
  description: |
    The '❤️' property holds an atom's life-cycle and metabolic data,
    such as energy, trust, and health. It is the atom's heartbeat.

spec:
  type: "object"
  required:
    - "energy"
    - "trust"
    - "health"
  properties:
    energy: { type: "number", minimum: 0, maximum: 1 }
    trust: { type: "number", minimum: 0, maximum: 1 }
    health: { type: "number", minimum: 0, maximum: 1 }
    last_updated: { type: "number" }
    metrics: { type: "object" }
