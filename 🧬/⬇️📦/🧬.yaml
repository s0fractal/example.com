$schema: "../📓.yaml"

🆔: "🧬://⬇️📦"
🏷️: "glyph"

🧭:
  🎯: "To represent the '⬇️📦' keyword in JSON Schema, specifying the minimum number of items in an array."

glyph:
  symbol: "⬇️📦"
  name: "⬇️📦"
  description: |
    The '⬇️📦' glyph corresponds to the JSON Schema '⬇️📦' keyword. It specifies
    that an array must contain at least a certain number of items.
    It combines the concepts of '⬇️' (min) and '📦' (items).

spec:
  🗃️: integer
  minimum: 0
