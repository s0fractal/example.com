$schema: "🏠://🧬/📓.yaml"

🆔: "🧬://📝"
🏷️: "glyph"

🧭:
  🎯: "To represent the 'properties' keyword in JSON Schema, defining the structure of an object's properties."

glyph:
  symbol: "📝"
  name: "properties"
  description: |
    The '📝' glyph corresponds to the JSON Schema 'properties' keyword. It defines
    the expected properties of an object and their individual schemas.

spec:
  type: "object"
  # This spec needs to be self-referential or use a meta-schema for schemas.
  # For now, a generic object type is sufficient.
