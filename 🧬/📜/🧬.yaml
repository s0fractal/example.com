$schema: "../../📓.yaml"

🆔: "🧬://📜"
kind: "glyph"

🧭:
  🎯: "To provide a formal, machine-readable specification for an atom."

glyph:
  symbol: "📜"
  description: |
    The '📜' property contains the formal specification of an atom, including
    references to its schema, type definitions, and internal constraints.

spec:
  type: "object"
  properties:
    schema: { type: "string" }
    types: { type: "object" }
    constraints: { type: "array", items: { type: "string" } }
