🌟: "https://json-schema.org/draft/2020-12/schema"
$path: "🏠://🧠/😇/🌐/📓.yaml"

$intent: "Narrows the general 😇 schema for the specific case of the Server (🌐) 😇, whose role is to serve the repo over HTTP."

🤝:
  # 1. Inherit common properties from the parent 😇 schema.
  - ➡️: "🏠://🧠/😇/📓.yaml"

  # 2. Add specific, narrowing constraints for the Server 😇.
  - 🗃️: object
    📝:
      id:
        🔒: "🧬://😇.server.v1"
      🧭: # Changed from intent
        📝:
          goal:
            🔒: "To serve the project repository over HTTP at example.com"
          role:
            🔒: "HTTP interface for the digital organism."
          conditions:
            🔒: ["on:demand"]
      spec:
        📝:
          schema:
            🔒: "🏠://🧠/😇/🌐/📓.yaml"
      runtime:
        📝:
          entrypoint:
            🔒: "🏠://🧠/😇/🌐/⚙️.ts"
          task_name:
            🔒: "serve"
    ✅:
      - id
