$schema: "../../📓.yaml"

🆔: "🧬://🌟"
🏷️: "glyph"

🎯:
  goal: "To represent the '$schema' keyword in JSON Schema, declaring which version of JSON Schema the schema is written against."

glyph:
  symbol: "🌟"
  name: "$schema"
  description: |
    The '🌟' glyph corresponds to the JSON Schema '$schema' keyword. It is used
    to declare which version of JSON Schema the schema is written against.

spec:
  type: "string"
  enum:
    - "http://json-schema.org/draft-07/schema#"
    - "https://json-schema.org/draft/2020-12/schema"
    # Add other supported schema versions as needed
