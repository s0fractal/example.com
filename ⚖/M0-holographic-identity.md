---
$path: "example.com/⚖/M0-holographic-identity.md"
---
# Principle: M0 Beacon (Holographic Identity)

Цей принцип, описаний у розділах **11.1** та **4.1** специфікації `kairos_git_binding.md`, гарантує, що кожна сутність (атом) у системі знає своє канонічне місце в топології.

---

### 1. Філософія: "Файл знає, де він живе"

У звичайних системах, якщо перемістити файл, його зв'язок із контекстом втрачається. У нашій системі ідентичність об'єкта нерозривно пов'язана з його місцем. Це робить дані **голографічними**: навіть якщо зруйнувати зовнішню структуру (папки), її можна повністю відновити з внутрішнього стану самих файлів.

**Формула:**
$$Identity = Content \times Context(Path)$$

### 2. Технічна реалізація (Embedded Path)

Кожен файл-атом **повинен** містити свій канонічний, абсолютний шлях **всередині себе**.

-   **Для `.seed.yaml` та `📓.yaml`:** Використовується зарезервоване поле `$path`.
    ```yaml
    # ~/example.com/λ/⚛/🆔/seed.yaml
    $path: "example.com/λ/⚛/🆔/seed.yaml"
    id: "seed://morphism.identity.v1"
    kind: "morphism"
    # ...
    ```

-   **Для Markdown (`.md`):** Використовується Frontmatter.
    ```markdown
    ---
    $path: example.com/λ/⚛/🆔/README.md
    ---
    # 🆔 (Identity Morphism)
    ```

### 3. Механізм Захисту (Auto-Fixer Reflex)

Щоб уникнути ручного редагування, система покладається на автоматичний `pre-commit` хук (рефлекс).

**Алгоритм:**

1.  Користувач виконує `git mv` для переміщення файлу або папки.
2.  Хук перехоплює дію перед комітом.
3.  Хук автоматично знаходить і оновлює поле `$path` всередині кожного зміненого файлу на новий, правильний шлях.

Це гарантує, що поле `$path` завжди є істинним відображенням реального стану файлової системи.
