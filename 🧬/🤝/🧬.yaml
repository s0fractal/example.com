$schema: "../../📓.yaml"

🆔: "🧬://🤝"
🏷️: "glyph"

🧭:
  🎯: "To represent the 'allOf' keyword in JSON Schema, specifying that an instance must validate against all given subschemas."

glyph:
  symbol: "🤝"
  name: "allOf"
  description: |
    The '🤝' glyph corresponds to the JSON Schema 'allOf' keyword. It combines
    multiple schemas, meaning the validated instance must satisfy all of them.

spec:
  type: "array"
  items:
    type: "object" # Each item is a schema object.
