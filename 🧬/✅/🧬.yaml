$schema: "🏠://🧬/📓.yaml"

🆔: "🧬://✅"
🏷️: "glyph"

🧭:
  🎯: "To represent the 'required' keyword in JSON Schema, listing properties that must be present."

glyph:
  symbol: "✅"
  name: "required"
  description: |
    The '✅' glyph corresponds to the JSON Schema 'required' keyword. It specifies
    an array of property names (or glyphs) that must be present in an object.

spec:
  type: "array"
  items:
    type: "string" # Could be glyphs, which are strings.
