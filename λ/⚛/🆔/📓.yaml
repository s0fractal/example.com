🌟: "https://json-schema.org/draft/2020-12/schema"
# The schema itself must know where it lives.
$path: "🏠://λ/⚛/🆔/📓.yaml"

# Custom Kairos/Sigma metadata for the Identity morphism.
$intent: "Narrows the general Morphism schema for the specific case of Identity (🆔)."
$evolve:
  on_error: "system:panic" # An error in Identity is a critical failure.

🤝:
  # 1. Inherit all common properties from the parent morphism schema.
  - ➡️: "🏠://λ/📓.yaml"

  # 2. Add specific, narrowing constraints for the Identity morphism.
  - 🗃️: object
    📝:
      # Enforce the M0 Beacon principle for the 🧬 file itself.
      "$path":
        🔒: "🏠://λ/⚛/🆔/🧬.yaml"
      id:
        ♾️: "^🧬://morphism\.identity\.v[0-9]+$"
      🔗: # links
        # Identity has no dependencies, so the array must be empty.
        ⬆️📦: 0
      🔌: # io
        📝:
          input: { ⬆️📦: 1, minItems: 1 }
          output: { ⬆️📦: 1, minItems: 1 }
      📜: # spec
        📝:
          📓: # Changed from schema
            🔒: "🏠://λ/⚛/🆔/📓.yaml"
          purity:
            🔒: "100% (referentially transparent)"
          category_theory:
            🔒: "Neutral element for function composition"
          layout:
            🗃️: object
            📝:
              strict: { 🗃️: boolean, 🔒: true }
              files:
                🗃️: array
                📦: { 🗃️: string }
                🔒:
                  - "🧬.yaml"
                  - "📓.yaml"
                  - "README.md"
                  - "proof.yaml"
              dirs:
                🗃️: array
                📦: { 🗃️: string }
                ⬆️📦: 0
        ✅:
          - 📓 # Changed from schema
          - purity
          - category_theory
          - layout
    ✅:
      - id
      - "$path"
