$schema: "🏠://🧬/📓.yaml"

🆔: "🧬://🔒"
🏷️: "glyph"

🧭:
  🎯: "To represent the 'const' keyword in JSON Schema, specifying a fixed value for a property."

glyph:
  symbol: "🔒"
  name: "const"
  description: |
    The '🔒' glyph corresponds to the JSON Schema 'const' keyword. It specifies
    that the value of a property must be exactly equal to the provided constant.

spec:
  # The type of 'const' can be anything, depending on the value.
  # So we define it as a generic JSON value.
  type:
    - "string"
    - "number"
    - "integer"
    - "boolean"
    - "object"
    - "array"
    - "null"
