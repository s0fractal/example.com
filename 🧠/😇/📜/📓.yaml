🌟: "https://json-schema.org/draft/2020-12/schema"
$path: "🏠://🧠/😇/📜/📓.yaml"

$intent: >
  This schema defines the structure for the Schema Guardian Angel (😇).
  It inherits properties from the general Angel schema and adds specific
  constraints relevant to schema management and optimization.

# Inherit from the general Angel schema
🤝:
  - ➡️: "🏠://🧠/😇/📓.yaml"

🗃️: object
📝:
  id:
    🔒: "🧬://😇.schema-guardian.v1"
  kind:
    🔒: "😇"
  🧭: # Changed from intent
    📝:
      goal:
        🔒: "Ensure all schemas (📓.yaml) in the system are consistent and optimized according to the glyph dictionary (📚)."
      role:
        🔒: "Maintainer and enforcer of schema integrity based on glyph definitions."
  spec:
    📝:
      schema:
        🔒: "🏠://🧠/😇/📜/📓.yaml"
      entrypoint:
        🔒: "🏠://🧠/😇/📜/reflex.ts"
      schedule:
        🗃️: string
        ♾️: "^(\*|([0-5]?\d)(?:,([0-5]?\d))*)\s+(\*|([0-5]?\d)(?:,([0-5]?\d))*)\s+(\*|([01]?\d|2[0-3])(?:,([01]?\d|2[0-3]))*)\s+(\*|([0-2]?\d|3[01])(?:,([0-2]?\d|3[01]))*)\s+(\*|([01]?\d|1[0-2])(?:,([01]?\d|1[0-2]))*)$" # Basic cron pattern
      config:
        🗃️: object
        📝:
          auto_optimize: { 🗃️: "boolean" }
          report_only_mode: { 🗃️: "boolean" }
✅:
  - id
  - kind
  - 🧭 # Changed from intent
  - spec
